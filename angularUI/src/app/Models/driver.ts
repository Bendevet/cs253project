export interface Driver{
    id: number,
    email?: string,
    fullname?: string,
    bikeInfo?: any,
    licenseNumber?: string,
    isAvailable?: boolean,
    driverLocation?: any,
    rating?: string,
    phone?: string
}